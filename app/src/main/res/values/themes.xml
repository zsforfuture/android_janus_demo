<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Janus_custom_master" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- dialog全屏样式-->
    <style name="common_DialogFullScreen" parent="Theme.AppCompat.Dialog">
        <item name="android:padding">0dp</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否显示title-->
        <item name="android:windowNoTitle">true</item>
        <!--是否是全屏-->
        <item name="android:windowFullscreen">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
    </style>


    <style name="common_DialogFullScreenCloseOnTouchOutside" parent="common_DialogFullScreen">
        <!--点击空白是否消失-->
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>


    <!--dialog底部弹出样式-->
    <style name="common_BottomDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/common_dialog_slide_up</item>
        <item name="android:windowExitAnimation">@anim/common_dialog_slide_down</item>
    </style>
</resources>